torchnlp.embeddings package
===========================

The ``torchnlp.embeddings`` package introduces embeddings that handle downloading, caching, loading,
and lookup. Embeddings ``torch.FloatTensor`` are looked up by token ``str``.

.. automodule:: torchnlp.embeddings
    :members:
